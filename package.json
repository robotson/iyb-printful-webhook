{
  "name": "iyb-printful-webhook",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npx dotenv-vault pull && mv .env .dev.vars && wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "currency.js": "^2.0.4",
    "stripe": "^12.1.1",
    "wrangler": "^2.16.0"
  },
  "devDependencies": {
    "dotenv-vault": "^1.19.0"
  }
}
